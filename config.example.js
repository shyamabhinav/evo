/**
 * EVO Client Configuration Template
 * 
 * To customize your local development settings:
 * 1. Duplicate this file and rename it to 'config.js'
 * 2. Add your Groq API Key (starts with gsk_...) or xAI API Key (starts with xai-...) below
 * 3. 'config.js' is git-ignored and will NEVER be committed to GitHub
 */
window.EVO_CONFIG = {
  // GroqCloud API Key (recommended) or xAI API Key
  GROK_API_KEY: "",

  // Optional custom proxy endpoint (default: direct upstream to Groq with native browser CORS)
  // API_ENDPOINT: "https://api.groq.com/openai/v1/chat/completions"
};
